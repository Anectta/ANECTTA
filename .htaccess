<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Permite acessar páginas sem a extensão .html
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME}\.html -f
  RewriteRule ^(.*)$ $1.html [L]
</IfModule>
